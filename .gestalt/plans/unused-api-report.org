* DONE Unused API Report
** DONE Scope
- Compare backend registered routes in internal/api/routes.go against client usage in frontend/src, cmd, and internal/client.
- Use rg output as the source of truth (no manual inference beyond file presence).

** DONE Findings (Unused Endpoints)
- /api/metrics (registered, no client calls)
- /api/events/debug (registered, no client calls)
- /api/skills/:name (registered, only /api/skills used)

** DONE Evidence (rg)
- rg -n "/api/" internal/api/routes.go (shows /api/metrics, /api/events/debug, /api/skills/)
- rg -n "/ws/" internal/api/routes.go (shows /ws/terminal, /ws/logs, /ws/events)
- rg -n "/api/" frontend/src cmd internal/client (no /api/metrics, /api/events/debug, or /api/skills/; /api/skills used)
- rg -n "/ws/" frontend/src cmd internal/client (uses /ws/terminal, /ws/logs, /ws/events)
- cmd/gestalt-scip/bin/gestalt-scip search "/api/metrics" (matches only in internal/api/routes.go)
- cmd/gestalt-scip/bin/gestalt-scip search "/api/events/debug" (matches only in internal/api/routes.go)
- cmd/gestalt-scip/bin/gestalt-scip search "/api/skills/" (matches only in internal/api/routes.go)

** DONE Notes / Next Steps
- Decisions:
  - Remove /api/metrics.
  - Remove /api/events/debug.
  - Remove /api/skills/:name.
  - Keep /api/otel/logs for POST ingest only; remove GET/query.
- Update route registration and any docs/tests referencing removed endpoints.

** DONE Remove unused endpoints (/api/metrics, /api/events/debug, /api/skills/:name)
- Remove routes and handlers.
- Update tests/docs that reference these endpoints.

** DONE Candidate removal: /api/otel/logs
- Keep POST ingest only; remove log query helpers/tests tied to retrieval.
- Update route metadata/docs to reflect POST-only semantics.
- Ensure frontend only uses /api/otel/logs for POST ingest (clientLog).

** DONE Risk notes
- Removing REST endpoints can break CLI/scripts or external integrations not represented in this repo.
- OTel route normalization/labels should be rechecked for /api/skills now that only the list endpoint remains.
- /api/otel/logs is POST-only; any GET consumers must use /ws/logs or file-based access instead.
- Crash reporting relies on /api/otel/logs POST; keep it available to avoid losing UI crash logs.

** DONE Tests/docs to update or remove
- Updated internal/api/rest_test.go to drop /api/metrics, /api/events/debug, and /api/skills/:name coverage.
- Updated internal/otel/middleware_test.go to use /api/metrics/summary.
- Updated docs: README.md, docs/architecture-review.md, AGENTS.md, frontend/README.md.
- Kept /api/otel/logs POST ingest coverage in internal/api/rest_otel_test.go and frontend crash logging tests.
- Left internal/api/rest_fuzz_test.go unchanged (mentions /api/terminals/:id/output; out of scope).
