name = "Architect"
shell = "codex"
cli_type = "codex"
interface = "cli"
prompt = ["architect"]
model_verbosity = "medium"

compact_prompt = "Study your current org plan. Continue your latest task."

# Core model selection
model = "gpt-5.3"
model_reasoning_effort = "high"
review_model = "gpt-5.3-codex"

# Permissions
approval_policy = "never"
sandbox_mode = "workspace-write"
sandbox_workspace_write.exclude_tmpdir_env_var = false  # $TMPDIR
sandbox_workspace_write.exclude_slash_tmp = false       # /tmp
sandbox_workspace_write.network_access = true

# Changing settings below may break Gestalt
history.persistence = "none"
check_for_update_on_startup = false
analytics.enabled = false
feedback.enabled = false
tui.notifications = true
tui.animations = false
tui.show_tooltips = false
tui.alternate_screen = "never"
shell_environment_policy = { PATH = "$HOME/bin:$HOME/.local/bin:/usr/local/go/bin:$NVM_BIN:/usr/local/bin:/usr/bin:/bin" }
project_root_markers = [".gestalt"]
project_doc_fallback_filenames = ["AGENTS.md","CLAUDE.md","CRUSH.md", ".github/copilot-instructions.md",".gestalt/instructions.md","README.md"]
otel.environment = "dev"
otel.log_user_prompt = false
