name = "Coder"
shell = "codex"
cli_type = "codex"
prompt = ["coder"]
skills = ["mcp-builder"]
llm_model = "default"

[cli_config]
# model = "o3" # values: any supported model id
# approval_policy = "on-request" # values: never | on-failure | untrusted | on-request
# sandbox_policy = "workspace-write" # values: read-only | workspace-write | danger-full-access
