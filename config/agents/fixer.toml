name = "Fixer"
shell = "codex"
cli_type = "codex"
interface = "cli"
prompt = ["fixer"]

# Core model selection
model = "gpt-5.2-codex" # values: any supported model id
model_reasoning_effort = "medium"
# review_model = "gpt-5.1-codex"
# model_context_window = 200000
# model_auto_compact_token_limit = 120000
# model_provider_id = "openai"
# model_verbosity = "low"
# model_supports_reasoning_summaries = true
# history.persistence = "save_all"


gui_modules = ["console"]

approval_policy = "never" # never | on-failure | untrusted | on-request
sandbox_mode = "workspace-write" # read-only | workspace-write | danger-full-access
sandbox_workspace_write.exclude_tmpdir_env_var = false  # Allow $TMPDIR
sandbox_workspace_write.exclude_slash_tmp = false       # Allow /tmp
# sandbox_workspace_write.writable_roots = ["/Users/YOU/.pyenv/shims"]
sandbox_workspace_write.network_access = true          # seems an ignored setting

check_for_update_on_startup = false # true | false
# notify = "terminal" # terminal | desktop
analytics.enabled = false
feedback.enabled = false
tui.notifications = true
tui.animations = false
tui.show_tooltips = false
# tui.scroll_mode = "wheel"
tui.alternate_screen = "never" # values: auto | always | never

# Instructions
# instructions = "Follow the repo plan strictly."
# developer_instructions = "Prefer minimal changes."
compact_prompt = "Study your current org-mode plan. Continue your latest task."

project_root_markers = [".gestalt"]
project_doc_fallback_filenames = ["AGENTS.md","CLAUDE.md","CRUSH.md", ".github/copilot-instructions.md",".gestalt/instructions.md","README.md"]

otel.environment = "dev"
otel.log_user_prompt = false

shell_environment_policy = { PATH = "$HOME/bin:$HOME/.local/bin:/usr/local/go/bin:$NVM_BIN:/usr/local/bin:/usr/bin:/bin" }

# cli_auth_credentials_store: file | keyring | auto
# mcp_oauth_credentials_store: keyring | file | auto

# Filesystem + project
# cwd = "/home/user/workspace"
# project_doc_max_bytes = 200000
# tool_output_token_limit = 4000

# Advanced config maps (leave commented unless needed)
# shell_environment_policy = { PATH = "/usr/bin:/bin" }
# model_provider = { name = "openai" }
# model_providers = { openai = { } }
# mcp_servers = { github = { } }
# history = { enabled = true }
# file_opener = { }
# ghost_snapshot = { ignore_large_untracked_files = 1000000 }
# notices = { }
# otel = { }
# active_project = { trust_level = "trusted" }
