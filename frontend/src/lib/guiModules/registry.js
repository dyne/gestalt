export const guiModuleRegistry = Object.freeze({
  terminal: { id: 'terminal' },
  console: { id: 'console' },
  'plan-progress': { id: 'plan-progress' },
})

export const guiModuleIds = Object.freeze(Object.keys(guiModuleRegistry))
